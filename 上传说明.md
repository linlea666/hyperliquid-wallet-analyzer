# 📤 上传压缩包说明

## 📦 压缩包位置

压缩包会在项目根目录生成，文件名格式：
```
gendan-deploy-YYYYMMDD-HHMMSS.zip
```

例如：`gendan-deploy-20241121-143000.zip`

**完整路径**：
```
/Users/huahua/Documents/gendan/gendan-deploy-*.zip
```

---

## 📤 上传到宝塔面板

### 方法一：通过宝塔面板文件管理（推荐）

1. **登录宝塔面板**
2. **进入「文件」管理**
3. **导航到 `/www/wwwroot/` 目录**
   - 点击左侧目录树中的 `/www/wwwroot/`
   - 或直接在路径栏输入 `/www/wwwroot/`
4. **点击「上传」按钮**
5. **选择压缩包文件**
   - 从本地选择 `gendan-deploy-*.zip` 文件
   - 等待上传完成
6. **解压文件**
   - 右键点击压缩包
   - 选择「解压」
   - 解压后会生成 `gendan` 文件夹
7. **删除压缩包**（可选）
   - 解压后可以删除压缩包节省空间

### 方法二：通过 SSH + SCP

```bash
# 在本地终端运行
cd /Users/huahua/Documents/gendan
scp gendan-deploy-*.zip root@your-server-ip:/www/wwwroot/

# 然后 SSH 登录服务器
ssh root@your-server-ip
cd /www/wwwroot
unzip gendan-deploy-*.zip
mv gendan-deploy-* gendan  # 重命名文件夹
```

---

## 📋 上传后目录结构

上传并解压后，目录结构应该是：

```
/www/wwwroot/gendan/
├── backend/
│   ├── app/
│   ├── requirements.txt
│   └── ...
├── frontend/
│   ├── src/
│   ├── package.json
│   └── ...
├── deploy.sh
├── nginx.conf.example
└── ...
```

---

## ✅ 验证上传

上传后，在宝塔面板「文件」管理中检查：

1. 进入 `/www/wwwroot/gendan`
2. 确认以下文件存在：
   - ✅ `backend/app/main.py`
   - ✅ `backend/requirements.txt`
   - ✅ `frontend/package.json`
   - ✅ `deploy.sh`

---

## 🚀 下一步

上传完成后，按照 **`宝塔面板图形化部署.md`** 的步骤继续部署。

