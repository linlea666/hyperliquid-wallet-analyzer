# HyperLiquid 钱包分析系统：生产级流程与功能蓝图

在现有架构审查的基础上，补充面向生产环境的流程、方案与新增功能，确保公开页与后台分离、SQLite 数据持久化可靠、评分/榜单可运营。

## 目标与原则
- **公开/后台分层**：公开访问的钱包列表、详情、榜单、异动页独立布局，无需登录；后台仅保留配置、导入、监控、审计功能。【F:frontend/src/router/index.js†L17-L112】
- **单一事实源于 SQLite**：配置、榜单规则、标签、审计全部入库，避免 JSON 浅合并/文件不一致。【F:backend/app/config.py†L20-L196】【F:backend/app/database.py†L18-L220】
- **可观察与可回滚**：每次变更（配置、榜单、标签、导入任务）都有审计、版本号与回滚能力。

## 端到端流程设计
1. **数据采集与入库**
   - 统一异步采集器：以队列消费地址/任务，落地 SQLite。表结构由迁移管理，默认 `PRAGMA foreign_keys=ON` 保证引用完整性。【F:backend/app/database.py†L26-L220】
   - 导入校验：批量导入前对地址格式、重复、数据源可达性做快速校验，失败记录可下载。

2. **配置与规则管理**
   - 配置表：`configs(key, version, payload, updated_by, updated_at)`；写入时使用 Pydantic 校验与乐观锁，保存后返回最新快照。
   - 榜单/标签/通知规则：单独表管理（如 `leaderboards`, `tags`, `notifications`），支持启停、版本与草稿发布。

3. **评分与分析流水线**
   - 使用任务表（`jobs`）跟踪评分/标签刷新，状态机：`pending -> running -> success/failed`，附带日志与重试次数。
   - 评分依据说明：在详情页暴露评分构成、权重与交易样本时间范围，消除“是否基于全量历史”的疑惑。

4. **公开展示链路**
   - **列表页**：展示 ROI、总盈亏、胜率、回撤、评分、趋势 sparkline、风险/风格标签、收藏/跟单 CTA。
   - **详情页**：净值/收益曲线、仓位分布、成交时间线、AI 评分解释、风险雷达与标签历史。
   - **榜单中心**：收益/稳健度/风格分榜单，支持区间筛选与导出；榜单数据从 SQLite 只读接口获取。
   - **异动/通知**：实时异动订阅（WebSocket），历史通知来自数据库，支持筛选与既读。

5. **后台运营闭环**
   - **榜单 CRUD**：在后台创建/编辑/排序规则，发布到公开榜单；支持预览与灰度发布。
   - **标签字典与自动规则**：维护标签集合，配置自动标签规则（如大额存取、成交频次）。
   - **导入任务中心**：展示批量导入进度、失败明细、重试与归档；与审计日志联动。
   - **配置与密钥管理**：安全区存储第三方密钥，配置改动写入 `audit_logs`，支持一键回滚。

6. **运维与质量保障**
   - **迁移与备份**：提供 `migrate` 命令管理 schema 版本，定时备份 SQLite，导出 CSV/JSON 便于审计。
   - **健康监控**：服务健康探针、队列/任务监控、API 慢查询日志、前端错误上报。
   - **测试矩阵**：
     - 契约测试：路由与 API 兼容公开/后台分层。
     - 数据一致性：配置写入后回读比对、榜单排序正确性。
     - 性能：热门榜单分页、详情页曲线查询的压测与缓存命中率。

## 新增功能清单（可迭代交付）
1. **路由与主题重构**：创建 `PublicLayout` 与 `AdminLayout`，调整公开页无需登录；统一深色玻璃拟态主题与组件库（指标卡/曲线/标签）。
2. **配置中心**：数据库驱动的配置页面，支持版本、草稿与发布、差异对比与回滚。
3. **榜单与标签运营工具**：后台榜单/标签 CRUD、发布/下架、自动标签规则、榜单权重实时调优。
4. **评分可解释性**：详情页展示评分拆解、历史曲线、样本覆盖区间与数据来源声明。
5. **导入/同步可靠性**：任务中心、失败重试、断点续传、导入校验报告、任务 webhook/通知。
6. **实时异动与通知中心**：WebSocket 实时推送+历史查询，阈值可配置，支持邮件/站内/浏览器提醒。
7. **审计与合规**：审计日志、操作留痕、敏感操作二次确认；关键表更新触发审计记录。
8. **可观测性**：前后端日志统一、慢查询分析、指标面板（QPS/错误率/延迟）、前端性能埋点。

## 参考交付节奏
- **Sprint 1**：路由拆分 + 配置中心入库 + 基础迁移/备份工具。
- **Sprint 2**：公开页视觉升级（列表/详情/榜单）+ 评分可解释性组件。
- **Sprint 3**：任务中心、实时异动/通知、审计与监控、性能优化与压测。

该蓝图补齐生产级流程、数据一致性与运营功能，为上线前的体验与可靠性打下基础。
