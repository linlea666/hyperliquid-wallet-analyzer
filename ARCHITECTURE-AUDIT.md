# HyperLiquid 钱包分析系统：架构与体验审查（补充版）

本轮审查聚焦之前暴露的问题（后台配置保存失败、公开页路由耦合、体验粗糙等），结合当前代码与 SQLite 存储基线，补充整体逻辑与落地方案。

## 现状与风险
- **前台/后台耦合导致公开页不可达**：所有业务路由（钱包列表、详情、通知等）被挂在 `AdminLayout` 下并默认需要登录，公开榜单/钱包页无法独立访问，且缺少单独的访客布局。【F:frontend/src/router/index.js†L17-L112】
- **配置管理仍是本地 JSON + 浅合并**：配置加载/更新直接写入文件夹 `data/config`，`update_config` 仅做浅层 `dict.update`，没有字段校验、版本控制或事务，容易出现保存成功但回读不一致的问题，也无法与 SQLite 数据保持一致性。【F:backend/app/config.py†L20-L196】
- **数据层缺乏仓储/迁移设计**：虽然数据库基于 SQLite，但数据访问以裸 SQL 嵌入 `Database` 类，缺少模型校验、迁移/版本机制和仓储层隔离，后续扩展（如新增榜单/标签规则）易破坏兼容性。【F:backend/app/database.py†L1-L160】
- **体验与评分模型脱节**：前端路由中没有榜单、评分分解、交易历史可视化等公开能力，仅有后台表单/表格页，无法呈现“全交易历史评分、风格画像”的卖点。【F:frontend/src/router/index.js†L17-L112】

## 设计与重构建议（结合 SQLite）
1. **信息架构分拆**
   - 建立 `PublicLayout`（`/`, `/wallets`, `/leaderboard`, `/alerts` 等）与 `/admin`（导入、配置、监控）的双布局路由，并拆分状态管理（公开页无登录依赖，仅共享只读数据服务）。
   - 公共组件（卡片、曲线、标签、评分雷达）抽象到 `components/analytics/*`，复用风格但隔离权限逻辑。

2. **配置/元数据入库与校验**
   - 在 SQLite 中新增 `configs`（key/version/payload）与 `audit_logs` 表，配置写入时使用 Pydantic 校验+版本号/更新时间戳；对外 API 返回最新版本及更新人，避免浅合并覆盖。
   - 后台保存后强制刷新配置快照，并在前端展示版本/校验提示，消除“保存不成功”体验。

3. **仓储层与迁移机制**
   - 为钱包、交易、标签、榜单等建立仓储类（封装 SQL + DTO），并引入最小迁移表（`schema_migrations`）记录版本；通过启动时校验/迁移脚本管理变更，确保 SQLite 数据结构可演进。

4. **公开页体验与数据闭环**
   - 钱包列表/详情：深色玻璃拟态主题，指标卡（ROI/回撤/胜率/评分）、迷你趋势线、风险/风格标签、跟单/收藏 CTA。详情页包含净值/收益曲线、仓位分布、交易轨迹、AI 评分解释。
   - 榜单/异动：提供收益、稳健度、风格分榜单与实时异动列表，支持区间筛选与导出；后台提供榜单规则 CRUD 与标签管理。
   - 评分模型透明化：前端展示评分构成与历史，明确“基于全量交易历史”的数据来源，避免用户困惑。

5. **可靠性与交互**
   - API 与 WebSocket 分层：行情/异动实时推送，其余查询走 REST + 分页缓存；全局错误兜底与空态设计。
   - 监控/日志：在后台提供任务队列状态、AI 调度、导入/标签任务进度视图，并与数据库审计日志关联。

6. **交付节奏（可落地路线图）**
   - 阶段 1：路由与布局重构 + 配置入库/校验；完成公开页只读数据链路。
   - 阶段 2：榜单、评分可视化、钱包详情视觉升级与组件库沉淀。
   - 阶段 3：异动/通知、AI 调度可视化、审计/监控闭环与性能优化。

以上方案在保持 SQLite 存储的同时补齐配置一致性、公开体验与数据可视化能力，为后续对标顶级设计与功能扩展打底。
