# HyperLiquid 钱包分析系统 - 部署和使用指南

## 📋 目录

1. [系统要求](#系统要求)
2. [本地开发环境](#本地开发环境)
3. [生产环境部署](#生产环境部署)
4. [使用指南](#使用指南)
5. [常见问题](#常见问题)
6. [维护和更新](#维护和更新)

---

## 系统要求

### 最低配置
- **CPU**: 2 核
- **内存**: 4GB
- **磁盘**: 20GB
- **操作系统**: Linux / macOS / Windows

### 推荐配置
- **CPU**: 4 核+
- **内存**: 8GB+
- **磁盘**: 50GB+ SSD
- **操作系统**: Ubuntu 20.04+ / CentOS 7+

### 软件依赖
- **Python**: 3.8+
- **Node.js**: 16+
- **Git**: 2.0+
- **Nginx**: 1.18+ (生产环境)

---

## 本地开发环境

### 1. 克隆项目

```bash
# 克隆仓库
git clone https://github.com/linlea666/hyperliquid-wallet-analyzer.git
cd hyperliquid-wallet-analyzer
```

### 2. 后端设置

#### 2.1 创建虚拟环境

```bash
cd backend
python3 -m venv venv

# macOS/Linux
source venv/bin/activate

# Windows
venv\Scripts\activate
```

#### 2.2 安装依赖

```bash
pip install -r requirements.txt
```

#### 2.3 初始化数据库

```bash
python init_db.py
```

#### 2.4 启动后端服务

```bash
# 开发模式（自动重载）
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# 或使用
python app/main.py
```

访问: http://localhost:8000
API 文档: http://localhost:8000/docs

### 3. 前端设置

#### 3.1 安装依赖

```bash
cd frontend
npm install
```

#### 3.2 配置环境变量

创建 `.env.development` 文件：

```bash
# API 基础 URL
VITE_API_BASE_URL=http://localhost:8000

# WebSocket URL
VITE_WS_URL=ws://localhost:8000/api/ws
```

#### 3.3 启动前端服务

```bash
npm run dev
```

访问: http://localhost:5173

### 4. 测试

```bash
# 后端测试
cd backend
python test_db.py          # 测试数据库
python test_scoring.py     # 测试评分系统
python test_websocket.py   # 测试 WebSocket

# 前端测试
cd frontend
npm run build              # 测试构建
```

---

## 生产环境部署

### 方案一：宝塔面板部署（推荐）

#### 1. 准备工作

1. **安装宝塔面板**
   ```bash
   # CentOS
   yum install -y wget && wget -O install.sh http://download.bt.cn/install/install_6.0.sh && sh install.sh
   
   # Ubuntu/Debian
   wget -O install.sh http://download.bt.cn/install/install-ubuntu_6.0.sh && sudo bash install.sh
   ```

2. **安装必要软件**
   - Python 3.8+
   - Nginx
   - PM2（可选）

#### 2. 部署后端

1. **在宝塔面板中添加 Python 项目**
   - 项目名称: `hyperliquid-backend`
   - 项目路径: `/www/wwwroot/gendan/backend`
   - Python 版本: 3.11
   - 启动文件: `app/main.py`
   - 端口: `8000`

2. **配置启动命令**
   ```bash
   cd /www/wwwroot/gendan/backend
   source venv/bin/activate
   uvicorn app.main:app --host 0.0.0.0 --port 8000
   ```

3. **初始化数据库**
   ```bash
   cd /www/wwwroot/gendan/backend
   source venv/bin/activate
   python init_db.py
   ```

#### 3. 部署前端

1. **本地构建**
   ```bash
   cd frontend
   
   # 修改 .env.production
   VITE_API_BASE_URL=https://kpl.17kx.net
   VITE_WS_URL=wss://kpl.17kx.net/api/ws
   
   # 构建
   npm run build
   ```

2. **上传到服务器**
   - 将 `frontend/dist` 目录上传到 `/www/wwwroot/gendan/frontend/dist`

3. **配置 Nginx**
   
   在宝塔面板中添加网站：
   - 域名: `kpl.17kx.net`
   - 根目录: `/www/wwwroot/gendan/frontend/dist`
   
   修改 Nginx 配置：
   ```nginx
   server {
       listen 80;
       server_name kpl.17kx.net;
       root /www/wwwroot/gendan/frontend/dist;
       index index.html;
       
       # 前端路由
       location / {
           try_files $uri $uri/ /index.html;
       }
       
       # API 反向代理
       location /api/ {
           proxy_pass http://127.0.0.1:8000;
           proxy_set_header Host $host;
           proxy_set_header X-Real-IP $remote_addr;
           proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
           proxy_set_header X-Forwarded-Proto $scheme;
       }
       
       # WebSocket 反向代理
       location /api/ws {
           proxy_pass http://127.0.0.1:8000;
           proxy_http_version 1.1;
           proxy_set_header Upgrade $http_upgrade;
           proxy_set_header Connection "upgrade";
           proxy_set_header Host $host;
           proxy_set_header X-Real-IP $remote_addr;
           proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
           proxy_read_timeout 86400;
       }
   }
   ```

4. **配置 SSL（推荐）**
   - 在宝塔面板中申请 Let's Encrypt 证书
   - 开启强制 HTTPS

#### 4. 启动服务

1. **启动后端**
   - 在宝塔面板的 Python 项目管理中启动项目
   - 或使用命令行：
     ```bash
     cd /www/wwwroot/gendan/backend
     source venv/bin/activate
     nohup uvicorn app.main:app --host 0.0.0.0 --port 8000 > logs/app.log 2>&1 &
     ```

2. **重启 Nginx**
   ```bash
   nginx -t
   nginx -s reload
   ```

3. **验证部署**
   - 访问: https://kpl.17kx.net
   - 检查 API: https://kpl.17kx.net/api/health
   - 检查文档: https://kpl.17kx.net/docs

### 方案二：Docker 部署（推荐用于团队）

#### 1. 创建 Dockerfile

**后端 Dockerfile** (`backend/Dockerfile`):
```dockerfile
FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

RUN python init_db.py

EXPOSE 8000

CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

**前端 Dockerfile** (`frontend/Dockerfile`):
```dockerfile
FROM node:18-alpine as builder

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .
RUN npm run build

FROM nginx:alpine

COPY --from=builder /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
```

#### 2. 创建 docker-compose.yml

```yaml
version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend/data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: unless-stopped
```

#### 3. 部署

```bash
# 构建镜像
docker-compose build

# 启动服务
docker-compose up -d

# 查看日志
docker-compose logs -f

# 停止服务
docker-compose down
```

---

## 使用指南

### 1. 首次登录

1. **访问系统**
   - URL: https://kpl.17kx.net
   
2. **登录账号**
   - 用户名: `admin`
   - 密码: `admin888`
   
3. **修改密码**（强烈建议）
   - 点击右上角用户头像
   - 选择"修改密码"
   - 输入旧密码和新密码

### 2. 批量导入钱包

1. **进入导入页面**
   - 侧边栏 → 钱包管理 → 批量导入

2. **选择导入方式**
   - **文本输入**: 直接粘贴钱包地址
   - **文件上传**: 上传 CSV 或 TXT 文件

3. **配置导入参数**
   - **批次大小**: 建议 50-100
   - **更新频率**: 
     - 高频（5分钟）: 活跃交易者
     - 普通（15分钟）: 一般交易者
     - 低频（1小时）: 不活跃交易者
   - **自动标签**: 建议开启

4. **开始导入**
   - 点击"开始导入"按钮
   - 实时查看进度
   - 等待完成

### 3. 查看钱包列表

1. **进入钱包列表**
   - 侧边栏 → 钱包管理 → 钱包列表

2. **筛选和排序**
   - 按评分筛选
   - 按标签筛选
   - 按时间排序

3. **查看详情**
   - 点击钱包地址
   - 查看完整信息

### 4. 系统配置

1. **基础配置**
   - 侧边栏 → 系统管理 → 系统配置 → 基础配置
   - 调整更新间隔
   - 启用/禁用调度器

2. **评分配置**
   - 系统配置 → 评分配置
   - 调整各维度权重
   - 确保总和为 100%

3. **通知配置**
   - 系统配置 → 通知配置
   - 配置邮件服务器（163邮箱示例）:
     - SMTP 服务器: `smtp.163.com`
     - SMTP 端口: `465`
     - 发件人邮箱: `your@163.com`
     - 邮箱密码: 使用授权码（非登录密码）
   - 添加收件人
   - 发送测试邮件

4. **AI 配置**
   - 系统配置 → AI 配置
   - 输入 DeepSeek API Key
   - 设置调用限制
   - 设置分析阈值

### 5. 日志管理

1. **查看日志**
   - 侧边栏 → 系统管理 → 日志管理

2. **筛选日志**
   - 按级别筛选
   - 按模块筛选
   - 按时间范围筛选
   - 关键词搜索

3. **实时日志**
   - 开启"实时日志"开关
   - 查看实时更新

4. **导出日志**
   - 点击"导出"按钮
   - 下载 CSV 文件

---

## 常见问题

### 1. 后端无法启动

**问题**: `ModuleNotFoundError: No module named 'xxx'`

**解决**:
```bash
cd backend
source venv/bin/activate
pip install -r requirements.txt
```

### 2. 前端无法连接后端

**问题**: API 请求失败

**解决**:
1. 检查后端是否启动
2. 检查环境变量配置
3. 检查 Nginx 反向代理配置
4. 检查防火墙设置

### 3. WebSocket 连接失败

**问题**: WebSocket 无法连接

**解决**:
1. 检查 Nginx WebSocket 配置
2. 确保使用 `wss://` (HTTPS) 或 `ws://` (HTTP)
3. 检查防火墙是否开放端口

### 4. 数据库锁定

**问题**: `database is locked`

**解决**:
```bash
# 重启后端服务
# 或检查是否有多个进程访问数据库
```

### 5. 导入失败

**问题**: 钱包导入失败

**解决**:
1. 检查地址格式（0x 开头，42 位）
2. 检查网络连接
3. 查看日志了解详细错误

### 6. 邮件发送失败

**问题**: 测试邮件发送失败

**解决**:
1. 检查 SMTP 配置
2. 使用授权码而非登录密码
3. 检查邮箱是否开启 SMTP 服务
4. 查看日志了解详细错误

---

## 维护和更新

### 1. 代码更新

#### 从 GitHub 拉取更新

```bash
# 进入项目目录
cd /www/wwwroot/gendan

# 拉取最新代码
git pull origin main

# 更新后端依赖
cd backend
source venv/bin/activate
pip install -r requirements.txt

# 更新前端依赖
cd ../frontend
npm install

# 重新构建前端
npm run build

# 重启服务
# 在宝塔面板中重启 Python 项目
# 或使用命令行重启
```

#### 推送更新到 GitHub

```bash
# 添加修改
git add .

# 提交
git commit -m "更新说明"

# 推送
git push origin main
```

### 2. 数据库备份

```bash
# 备份数据库
cp backend/data/hyperliquid_analyzer.db backend/data/backup_$(date +%Y%m%d).db

# 定期备份（添加到 crontab）
0 2 * * * cp /www/wwwroot/gendan/backend/data/hyperliquid_analyzer.db /www/wwwroot/gendan/backend/data/backup_$(date +\%Y\%m\%d).db
```

### 3. 日志清理

```bash
# 清理旧日志（保留最近 30 天）
find backend/logs -name "*.log" -mtime +30 -delete

# 定期清理（添加到 crontab）
0 3 * * 0 find /www/wwwroot/gendan/backend/logs -name "*.log" -mtime +30 -delete
```

### 4. 性能监控

```bash
# 查看后端进程
ps aux | grep uvicorn

# 查看内存使用
free -h

# 查看磁盘使用
df -h

# 查看数据库大小
du -sh backend/data/hyperliquid_analyzer.db
```

### 5. 日志查看

```bash
# 查看后端日志
tail -f backend/logs/app.log

# 查看 Nginx 日志
tail -f /www/wwwlogs/kpl.17kx.net.log

# 查看错误日志
tail -f /www/wwwlogs/kpl.17kx.net.error.log
```

---

## 安全建议

### 1. 密码安全
- ✅ 首次登录后立即修改默认密码
- ✅ 使用强密码（字母+数字+特殊字符）
- ✅ 定期更换密码

### 2. 服务器安全
- ✅ 配置防火墙
- ✅ 禁用 root 登录
- ✅ 使用 SSH 密钥认证
- ✅ 定期更新系统

### 3. 应用安全
- ✅ 启用 HTTPS
- ✅ 配置 CORS
- ✅ 限制 API 访问频率
- ✅ 定期备份数据

### 4. 数据安全
- ✅ 定期备份数据库
- ✅ 加密敏感信息
- ✅ 限制数据库访问
- ✅ 监控异常访问

---

## 技术支持

### 文档
- GitHub: https://github.com/linlea666/hyperliquid-wallet-analyzer
- API 文档: https://kpl.17kx.net/docs

### 常用命令

```bash
# 查看后端状态
systemctl status hyperliquid-backend

# 重启后端
systemctl restart hyperliquid-backend

# 查看日志
journalctl -u hyperliquid-backend -f

# 测试 Nginx 配置
nginx -t

# 重启 Nginx
systemctl restart nginx
```

---

## 附录

### A. 163 邮箱 SMTP 配置

1. **开启 SMTP 服务**
   - 登录 163 邮箱
   - 设置 → POP3/SMTP/IMAP
   - 开启 SMTP 服务
   - 获取授权码

2. **配置参数**
   - SMTP 服务器: `smtp.163.com`
   - SMTP 端口: `465` (SSL) 或 `25` (非 SSL)
   - 发件人邮箱: `your@163.com`
   - 密码: 使用授权码

### B. 常用端口

| 服务 | 端口 | 说明 |
|------|------|------|
| 前端 | 80/443 | HTTP/HTTPS |
| 后端 API | 8000 | FastAPI |
| WebSocket | 8000 | 与 API 同端口 |
| SSH | 22 | 服务器管理 |

### C. 系统路径

| 项目 | 路径 |
|------|------|
| 项目根目录 | `/www/wwwroot/gendan` |
| 后端代码 | `/www/wwwroot/gendan/backend` |
| 前端代码 | `/www/wwwroot/gendan/frontend/dist` |
| 数据库 | `/www/wwwroot/gendan/backend/data` |
| 日志 | `/www/wwwroot/gendan/backend/logs` |
| Nginx 配置 | `/www/server/panel/vhost/nginx` |

---

*最后更新: 2025-11-22*
*版本: V2.0*

