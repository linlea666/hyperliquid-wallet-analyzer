# 快速修复并启动

## 🔧 当前问题

npm 仍然在使用代理 `127.0.0.1:7890`，但代理服务器未运行。

## ✅ 快速修复步骤

### 方法一：直接修复 npm 配置（推荐）

在终端运行：

```bash
# 1. 清除 npm 代理配置
npm config delete proxy
npm config delete https-proxy

# 2. 设置国内镜像
npm config set registry https://registry.npmmirror.com

# 3. 清除环境变量中的代理
unset http_proxy https_proxy HTTP_PROXY HTTPS_PROXY

# 4. 重新启动
cd /Users/huahua/Documents/gendan
./start.sh
```

### 方法二：手动安装前端依赖

如果方法一不行，手动安装：

```bash
cd /Users/huahua/Documents/gendan/frontend

# 清除代理
unset http_proxy https_proxy HTTP_PROXY HTTPS_PROXY
npm config delete proxy
npm config delete https-proxy
npm config set registry https://registry.npmmirror.com

# 安装依赖
npm install

# 然后运行启动脚本
cd ..
./start.sh
```

### 方法三：检查后端是否已启动

即使前端安装失败，后端可能已经启动了：

```bash
# 检查后端是否运行
curl http://localhost:8000/health

# 如果返回 {"status":"ok"}，说明后端已启动
# 可以访问 API 文档: http://localhost:8000/docs
```

---

## 🚀 启动后访问

启动成功后：

1. **前端界面**: http://localhost:5173
2. **后端 API**: http://localhost:8000
3. **API 文档**: http://localhost:8000/docs

---

## 📋 宝塔面板部署（服务器）

如果要在服务器上部署，请参考：

1. **上传项目文件**到服务器 `/www/wwwroot/gendan`
2. **运行部署脚本**：
```bash
chmod +x deploy.sh
./deploy.sh
```

3. **配置 Nginx**（参考 `宝塔面板部署指南.md`）

详细步骤请查看：**`宝塔面板部署指南.md`**

