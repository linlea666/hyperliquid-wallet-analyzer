# 🚀 立即启动指南

## ⚠️ 当前问题

npm 安装依赖时遇到代理问题（127.0.0.1:7890 不可用）

## ✅ 解决方案（3步搞定）

### 步骤 1: 清除 npm 代理配置

在终端运行：

```bash
npm config delete proxy
npm config delete https-proxy
npm config set registry https://registry.npmmirror.com
```

### 步骤 2: 清除环境变量代理

```bash
unset http_proxy https_proxy HTTP_PROXY HTTPS_PROXY
```

### 步骤 3: 手动安装前端依赖（如果还没安装）

```bash
cd /Users/huahua/Documents/gendan/frontend
npm install
```

### 步骤 4: 启动服务

```bash
cd /Users/huahua/Documents/gendan
./start.sh
```

---

## 🎯 一键修复脚本

我已经创建了修复脚本，直接运行：

```bash
cd /Users/huahua/Documents/gendan
./fix-proxy.sh
./start.sh
```

---

## 📊 启动后访问

启动成功后，在浏览器打开：

- **前端界面**: http://localhost:5173
- **后端 API**: http://localhost:8000  
- **API 文档**: http://localhost:8000/docs

---

## 🔍 检查服务状态

### 检查后端是否运行

```bash
curl http://localhost:8000/health
```

如果返回 `{"status":"ok"}`，说明后端已启动。

### 检查前端是否运行

```bash
curl http://localhost:5173
```

### 查看日志

```bash
# 后端日志
tail -f logs/backend.log

# 前端日志
tail -f logs/frontend.log
```

---

## 🛑 停止服务

```bash
cd /Users/huahua/Documents/gendan
./stop.sh
```

---

## 📋 宝塔面板部署（服务器）

如果要在服务器上部署，请参考：

**`宝塔面板部署指南.md`**

主要步骤：
1. 上传项目到服务器 `/www/wwwroot/gendan`
2. 运行 `./deploy.sh` 一键部署
3. 配置 Nginx 反向代理

---

## 💡 提示

- 如果 npm install 仍然失败，检查网络连接
- 确保端口 8000 和 5173 未被占用
- 查看日志文件排查问题

