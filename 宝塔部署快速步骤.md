# ⚡ 宝塔部署快速步骤（5分钟）

## 🎯 核心步骤

### 1️⃣ 克隆代码（1分钟）

在宝塔「文件」→「终端」运行：

```bash
cd /www/wwwroot
git clone https://github.com/linlea666/hyperliquid-wallet-analyzer.git gendan
cd gendan
```

---

### 2️⃣ 配置后端（2分钟）

```bash
# 创建虚拟环境
python3 -m venv venv
source venv/bin/activate

# 安装依赖
pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r backend/requirements.txt

# 复制配置文件
cp backend/data/config/system.json.example backend/data/config/system.json
```

---

### 3️⃣ 配置前端（1分钟）

```bash
cd frontend
npm config set registry https://registry.npmmirror.com
npm install
npm run build
```

---

### 4️⃣ 宝塔面板配置（1分钟）

#### A. 添加 Python 项目

1. 「Python项目」→「添加Python项目」
2. 填写：
   - 项目名称：`gendan-backend`
   - 项目路径：`/www/wwwroot/gendan`
   - Python版本：3.8+
   - 启动文件：`backend/app/main.py`
   - 端口：`8000`
   - 虚拟环境：`/www/wwwroot/gendan/venv`
3. 启动命令：`uvicorn app.main:app --host 0.0.0.0 --port 8000`
4. 点击「启动」

#### B. 添加网站

1. 「网站」→「添加站点」
2. 填写：
   - 域名：您的域名或 IP
   - 根目录：`/www/wwwroot/gendan/frontend/dist`
3. 「设置」→「反向代理」→「添加」
   - 目标URL：`http://127.0.0.1:8000`
   - 代理名称：`api`
4. 「配置文件」中添加：

```nginx
location /api {
    proxy_pass http://127.0.0.1:8000;
    proxy_set_header Host $host;
}
```

---

### 5️⃣ 测试

- 后端：`http://您的IP:8000/docs`
- 前端：`http://您的IP`

---

## ✅ 完成！

详细步骤请查看：`宝塔面板完整部署教程-小白版.md`

