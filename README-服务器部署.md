# 🚀 服务器部署快速指南

## 🎯 推荐方式：使用宝塔面板图形界面（最简单）

### 📦 第一步：打包项目文件（本地）

```bash
cd /Users/huahua/Documents/gendan
./打包部署文件.sh
```

### 📤 第二步：上传到服务器

1. 宝塔面板 → 「文件」→ `/www/wwwroot/`
2. 上传 `gendan-deploy-*.zip` 并解压
3. 重命名为 `gendan`

### 🐍 第三步：添加 Python 项目（后端）

1. 「软件商店」→ 安装「Python项目管理器」
2. 「Python项目管理器」→ 「添加项目」

**配置信息**：
- 项目名称：`hyperliquid-backend`
- Python环境：3.11.13+
- 启动方式：**命令行启动**
- 项目路径：`/www/wwwroot/gendan/backend`
- 启动命令：`uvicorn app.main:app --host 0.0.0.0 --port 8000`
- 通讯协议：**asgi** ⚠️ 重要！
- 端口：`8000`
- 安装依赖包：`/www/wwwroot/gendan/backend/requirements.txt`

3. 点击「确定」→ 「模块」→ 安装依赖 → 「启动」

### 📦 第四步：构建前端

1. 「文件」→ `/www/wwwroot/gendan/frontend` → 「终端」
2. 运行：
```bash
npm config set registry https://registry.npmmirror.com
npm install
npm run build
```

### 🌐 第五步：创建网站并配置反向代理

1. 「网站」→ 「添加站点」
   - 域名：您的域名或 IP
   - 根目录：`/www/wwwroot/gendan/frontend/dist`
   - PHP：纯静态

2. 网站「设置」→ 「反向代理」→ 「添加反向代理」
   - 代理名称：`api`
   - 目标URL：`http://127.0.0.1:8000`
   - 代理目录：`/api`

3. 网站「设置」→ 「配置文件」
   - 在 `location /` 中添加：`try_files $uri $uri/ /index.html;`
   - 保存并重启 Nginx

---

## 📚 详细文档

- **`宝塔面板图形化部署.md`** - 图形界面详细步骤 ⭐ 推荐
- **`宝塔面板快速部署.md`** - 5分钟快速指南
- **`服务器部署完整方案.md`** - 完整部署方案（含脚本方式）

## ✅ 第五步：验证部署

访问：
- **前端**: `http://your-domain.com`
- **API**: `http://your-domain.com/api/health`
- **文档**: `http://your-domain.com/api/docs`

## 🔄 后续更新代码

### 本地开发后更新服务器

1. **本地修改代码并测试**

2. **打包更新文件**：
```bash
cd /Users/huahua/Documents/gendan
zip -r update.zip backend/app frontend/src \
  -x "*.git*" -x "node_modules/*"
```

3. **上传到服务器**并解压

4. **服务器更新**：
```bash
cd /www/wwwroot/gendan
unzip -o update.zip
cd frontend && npm run build && cd ..
pm2 restart hyperliquid-backend
```

## 📝 服务管理

```bash
# 查看状态
pm2 status

# 查看日志
pm2 logs hyperliquid-backend

# 重启服务
pm2 restart hyperliquid-backend

# 停止服务
pm2 stop hyperliquid-backend
```

## 📚 详细文档

- **`服务器部署完整方案.md`** - 完整部署步骤
- **`宝塔面板部署指南.md`** - 宝塔面板详细配置
- **`nginx.conf.example`** - Nginx 配置示例

---

**部署完成后，系统就可以使用了！** 🎉

