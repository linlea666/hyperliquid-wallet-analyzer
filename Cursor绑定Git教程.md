# Cursor 绑定 Git 教程

## 🎯 为什么要绑定 Git？

绑定 Git 后，您可以在 Cursor 中：
- ✅ 直接查看文件修改状态
- ✅ 一键提交和推送代码
- ✅ 查看代码历史
- ✅ 同步代码更方便

---

## 📋 第一步：推送代码到 GitHub

### 方式一：使用脚本（推荐）

在终端运行：

```bash
cd /Users/huahua/Documents/gendan
./推送代码到GitHub.sh
```

### 方式二：手动操作

在终端运行：

```bash
cd /Users/huahua/Documents/gendan

# 1. 初始化 Git（如果还没有）
git init

# 2. 添加所有文件
git add .

# 3. 提交
git commit -m "Initial commit: HyperLiquid 钱包分析系统"

# 4. 设置主分支
git branch -M main

# 5. 添加远程仓库
git remote add origin https://github.com/linlea666/hyperliquid-wallet-analyzer.git

# 6. 推送代码
git push -u origin main
```

**如果提示输入用户名密码**：
- Username: `linlea666`
- Password: **直接回车**（Public 仓库不需要密码）

---

## 🔗 第二步：在 Cursor 中绑定 Git

### 2.1 Cursor 自动检测

Cursor 会自动检测 Git 仓库，如果项目目录中有 `.git` 文件夹，Cursor 会自动启用 Git 功能。

### 2.2 查看 Git 状态

1. **打开 Cursor**
2. **打开项目**：`/Users/huahua/Documents/gendan`
3. **查看左侧边栏**：
   - 应该能看到「Source Control」图标（类似分支图标）
   - 如果有修改的文件，会显示数字

### 2.3 配置 Git 用户信息（首次使用需要）

在 Cursor 终端运行：

```bash
git config --global user.name "您的名字"
git config --global user.email "您的邮箱"
```

例如：
```bash
git config --global user.name "linlea666"
git config --global user.email "your-email@example.com"
```

---

## 📝 第三步：在 Cursor 中使用 Git

### 3.1 查看修改

1. **点击左侧「Source Control」图标**（或按 `Ctrl+Shift+G`）
2. **查看修改的文件**：
   - 绿色 `M`：已修改
   - 绿色 `A`：新添加
   - 红色 `D`：已删除

### 3.2 提交代码

1. **在「Source Control」面板**
2. **在「Message」输入框**输入提交说明，如：
   ```
   更新了钱包分析功能
   ```
3. **点击「Commit」按钮**（或按 `Cmd+Enter`）
4. **点击「Sync Changes」**（同步到 GitHub）

### 3.3 推送代码

**方法一：使用 Sync Changes**
- 点击「Source Control」面板右上角的「Sync Changes」按钮
- 会自动 pull 和 push

**方法二：使用命令面板**
1. 按 `Cmd+Shift+P`（Mac）或 `Ctrl+Shift+P`（Windows）
2. 输入 `Git: Push`
3. 选择并执行

**方法三：使用终端**
```bash
git push origin main
```

---

## 🔄 日常更新流程（在 Cursor 中）

### 修改代码后：

1. **修改代码**
2. **查看修改**：
   - 点击「Source Control」图标
   - 查看修改的文件
3. **提交代码**：
   - 在「Message」输入提交说明
   - 点击「Commit」
4. **推送到 GitHub**：
   - 点击「Sync Changes」
   - 或运行：`git push origin main`

---

## 🎯 Cursor Git 功能说明

### Source Control 面板功能

- **Changes**：显示修改的文件
- **Staged Changes**：已暂存的文件
- **Message**：提交说明输入框
- **Commit**：提交按钮
- **Sync Changes**：同步到 GitHub（pull + push）
- **...**：更多操作菜单

### 常用操作

1. **查看文件差异**：
   - 点击修改的文件
   - 会显示修改前后的对比

2. **暂存文件**：
   - 点击文件旁边的「+」号
   - 或右键选择「Stage Changes」

3. **撤销修改**：
   - 右键文件 → 「Discard Changes」

4. **查看历史**：
   - 右键文件 → 「Open Timeline」
   - 或使用命令：`Git: View History`

---

## 📋 完整工作流程

### 日常开发流程：

```
1. 在 Cursor 中修改代码
   ↓
2. 查看修改（Source Control 面板）
   ↓
3. 输入提交说明
   ↓
4. 点击 Commit
   ↓
5. 点击 Sync Changes（推送到 GitHub）
   ↓
6. 服务器运行 ./update.sh（更新服务器代码）
```

---

## ✅ 验证绑定

### 检查 Git 配置

在 Cursor 终端运行：

```bash
git remote -v
```

应该显示：
```
origin  https://github.com/linlea666/hyperliquid-wallet-analyzer.git (fetch)
origin  https://github.com/linlea666/hyperliquid-wallet-analyzer.git (push)
```

### 检查 GitHub

访问：https://github.com/linlea666/hyperliquid-wallet-analyzer

应该能看到所有项目文件。

---

## 💡 提示

1. **Cursor 会自动检测 Git**：打开项目后自动启用
2. **首次使用需要配置用户信息**：`git config --global user.name` 和 `user.email`
3. **Public 仓库推送简单**：不需要密码
4. **Sync Changes 最方便**：一键同步到 GitHub

---

## 🎉 完成！

现在您可以：
- ✅ 在 Cursor 中修改代码
- ✅ 一键提交和推送
- ✅ 代码自动同步到 GitHub
- ✅ 服务器一键更新

**开始使用吧！** 🚀

