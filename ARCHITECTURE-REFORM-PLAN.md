# HyperLiquid 钱包分析系统重构方案（架构视角）

## 目标
- 将项目从“原型+零散特性”升级为“可观测、可扩展、可运营”的生产级平台。
- 面向公开门户与后台运营双场景，统一数据层（SQLite/可迁移 DB），提升可靠性、安全性与用户体验。
- 以分阶段可交付为准则：先拆分信息架构与数据治理，再补齐能力闭环与体验升级。

## 现状诊断（重点问题）
- **路由与权限耦合**：公开页与后台共用 `AdminLayout`，导致公开内容需登录且导航混乱。
- **配置存储脆弱**：配置与业务数据混放本地 JSON/SQLite，缺乏模式校验、版本控制与并发保护，导致“保存成功但读出异常”。
- **数据口径不一致**：榜单、评分、导入、监控等模块各自查询/计算，缺少统一视图（如标准化收益/风险/活跃度指标）。
- **前后端协同弱**：前端缺少状态管理与错误兜底，后台 API 缺少契约/文档与测试，导致加载失败与不可观测。
- **体验与示例落差大**：桌面端视觉、指标呈现、交互（筛选/排序/趋势）均与目标风格不符，缺少风险提示与标签体系透出。

## 目标架构（分层与职责）
- **入口层**：
  - `/` 公开门户（无需登录）：钱包榜单、钱包详情、趋势&异动流、榜单配置透出（只读）。
  - `/admin` 运营后台（登录保护）：数据管理、配置中心、榜单/标签/通知/导入、监控与日志。
- **接口层（FastAPI）**：
  - REST：查询/配置/管理；WebSocket：实时行情与异动推送；OpenAPI/SDK 自动生成。
  - 路由分区：`public`（只读）、`admin`（写入/敏感），通过依赖注入校验角色。
- **服务层**：
  - `wallet_service`：统一钱包画像、指标计算、缓存；支持分页/排序/筛选。
  - `scoring_service`：基于全量交易历史的评分/标签生成，暴露分层指标（收益/风险/行为）。
  - `ranking_service`：榜单编排（字段、权重、周期），支持后台 CRUD 与草稿/发布。
  - `ingestion_service`：导入、校验、幂等写入；批处理/任务状态跟踪。
  - `monitoring_service`：系统/任务/数据质量监控，触发通知/告警。
- **数据层（SQLite 起步，可迁移 Postgres）**：
  - 规范化 schema：`wallets`、`transactions`、`scores`、`tags`、`rankings`、`imports`、`configs`、`audit_logs`。
  - 数据视图：标准化指标视图（ROI、PnL、回撤、胜率、活跃度）+ 物化视图缓存热点榜单。
  - 配置表引入 `version` 与 `updated_by/at`，支持乐观锁与审计。
- **前端（Vite+React）**：
  - 路由拆分：`/portal`（公开布局）、`/admin`（受控布局）；共享组件库（卡片/图表/标签/提示）。
  - 状态管理：React Query + Zustand/Context；错误与空态兜底；骨架屏与重试。
  - 可视化：ECharts 主题（深色+玻璃拟态），指标卡、微型趋势图、风险雷达图。

## 数据与配置治理
- **配置中心**：
  - 定义 Pydantic 模型，保存到 `configs` 表；读写使用版本号+事务；提供审计与回滚接口。
  - 支持“草稿-发布”流程，后台保存不直接生效，可预览/差异对比。
- **数据导入/标签**：
  - 导入任务记录 `status/progress/error`，结果落库并触发标签/评分增量计算。
  - 标签字典与自动规则表（条件→标签），后台可管理并即时生效。
- **评分/榜单**：
  - 评分基于全量交易历史，拆分维度（收益、风险、稳定性、活跃度、风格），前端展示权重与贡献度。
  - 榜单配置化：排序字段、周期、过滤条件、展示列；支持定时刷新与缓存。

## 可靠性与安全
- **API 契约与测试**：
  - OpenAPI 校验 + Pydantic Schema；为核心接口编写契约测试（配置 CRUD、榜单查询、导入流程、评分计算）。
  - 引入 ruff/mypy & 前端 ESLint/TypeScript 严格模式。
- **并发与幂等**：
  - 配置写入使用乐观锁；导入任务 idempotency key；任务/调度器采用 APScheduler 单实例锁或 DB 分布式锁。
- **安全**：
  - JWT+RBAC：公开路由只读，后台路由校验角色；审计日志记录敏感操作。
  - 输入校验与速率限制，WebSocket 订阅白名单。

## 可观测性与运维
- **日志与审计**：结构化日志（traceId/spanId），审计表记录用户/动作/差异。
- **指标**：采集 FastAPI、任务、DB、缓存指标导出 Prometheus；关键指标告警（导入失败率、调度延迟、榜单刷新时长）。
- **数据质量**：定期校验交易/余额/标签一致性；异常自动工单/通知。
- **部署**：
  - 以 SQLite 为单机默认，预留 Postgres 迁移脚本；分环境配置（dev/stage/prod）。
  - Docker 镜像分层构建 + 健康检查；CI 自动单测/格式化/镜像扫描。

## 交付路线（建议 3 迭代）
1. **信息架构与配置稳固**：拆分路由、接入 JWT+RBAC、配置中心落库+版本化、导入任务化+幂等；补充契约测试。
2. **能力闭环与指标统一**：实现统一指标视图、评分/榜单服务、标签规则管理、监控指标与告警；前端接入 React Query 与错误兜底。
3. **体验与性能升级**：对齐视觉语言（深色+玻璃拟态）、完善榜单/详情可视化、WebSocket 实时异动流、缓存/物化视图优化、可观测性仪表盘。

## 成功标准
- 公开页与后台权限清晰，配置变更可回滚且无读写竞态；
- 榜单/评分/标签/导入链路闭环可观测，错误可追踪；
- 桌面端体验对标示例风格，指标与风险展示全面；
- CI 通过、监控与告警上线，具备生产可运维性。
